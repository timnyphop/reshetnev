import { HostTree } from '@angular-devkit/schematics';
export declare class NgMorphTree extends HostTree {
    private readonly hostSink;
    constructor(root?: string);
    commitChanges(): Promise<void>;
}
