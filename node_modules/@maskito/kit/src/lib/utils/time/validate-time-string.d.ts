import type { MaskitoTimeMode, MaskitoTimeSegments } from '../../types';
export declare function validateTimeString({ timeString, paddedMaxValues, offset, selection: [from, to], timeMode, }: {
    timeString: string;
    paddedMaxValues: MaskitoTimeSegments;
    offset: number;
    selection: readonly [number, number];
    timeMode: MaskitoTimeMode;
}): {
    validatedTimeString: string;
    updatedTimeSelection: [number, number];
};
//# sourceMappingURL=validate-time-string.d.ts.map