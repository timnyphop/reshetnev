import type { TypedInputEvent } from '../../types';
export declare class EventListener {
    private readonly element;
    private readonly listeners;
    constructor(element: HTMLElement);
    listen<E extends keyof HTMLElementEventMap>(eventType: E, fn: (event: E extends 'beforeinput' | 'input' ? TypedInputEvent : HTMLElementEventMap[E]) => unknown, options?: AddEventListenerOptions): void;
    destroy(): void;
}
//# sourceMappingURL=event-listener.d.ts.map